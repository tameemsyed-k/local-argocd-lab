apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: pathnex-nginx
  namespace: argocd
spec:
  project: default
  source:
    repoURL: 'https://github.com/pathnex/pathnex-argocd_lab.git'
    targetRevision: main
    path: nginx
    helm:
      valueFiles:
        - values.yaml
  destination:
    server: 'https://kubernetes.default.svc'
    namespace: pathnex-nginx
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
